<div class="container">
<h1 class="mt-4"><%= gravatar_for @user %> <%= @user.username %></h1>
</div>

<div class="container">
<div class="mt-3">
  <%= link_to "Edit your profile", edit_user_path(@user), class:"btn btn-info" %>
</div>
</div>

<div class="container">
  <h3 class="mt-4">Shows</h3>
</div>
<%= render 'shared/pagination' %>
<%= render 'articles/article' %>
<%= render 'shared/pagination' %>
